//Our initialState and reducers are together here for convenience.
//this is not how the standards look, but we felt it would be easier to understand if all of the logic was placed here as it is a simple app.

const initialState = {

}

export default function reducer(state = initialState, action) {
    switch (action.type) {

    }
    return state
}